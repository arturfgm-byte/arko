# Как заменить сайт на ar-ko.ru — пошагово

Инструкция написана 09.08.2026 для самостоятельной заливки через панель reg.ru.
Копия лежит на рабочем столе: «КАК ЗАЛИТЬ САЙТ.html» (открывается двойным кликом).

DNS и почту трогать не нужно — они не меняются. Меняются только файлы сайта.

## Что понадобится

- Логин и пароль от личного кабинета reg.ru
- Файл **«АРКО-сайт-для-хостинга.zip»** с рабочего стола (12 МБ)

## Шаг 1. Открыть файлы сайта

1. Зайти на [reg.ru](https://www.reg.ru/) → «Войти».
2. Раздел **«Хостинг»** (или «Услуги» → «Хостинг») → у услуги нажать **«Перейти в панель управления»**.
3. В открывшейся панели найти **«Менеджер файлов»** (может называться «Файловый менеджер»).
4. Пройти по папкам до сайта: `www` → `ar-ko.ru`.
   Признак, что папка правильная: внутри лежит файл **index.html** и папки вроде `tilda`, `images`.

## Шаг 2. Показать скрытые файлы

В настройках менеджера файлов (шестерёнка или меню «Вид») включить **«Показывать скрытые файлы»**.
Без этого не будет виден файл `.htaccess` — он отвечает за переход на https, и его легко потерять.

## Шаг 3. Сохранить старый сайт

**Ничего не удалять.** Старое просто убирается в сторону:

1. Кнопка **«Создать» → «Каталог»**, имя: `_old`
2. Выделить всё остальное содержимое папки (кроме `_old` и, если есть, `cgi-bin`).
3. **«Вырезать»** → зайти в `_old` → **«Вставить»**.

Теперь папка сайта пустая, а старая версия целиком лежит в `_old`.

## Шаг 4. Загрузить новый сайт

1. Вернуться в папку `ar-ko.ru` (кнопка «Вверх»).
2. **«Закачать»** («Загрузить») → выбрать **«АРКО-сайт-для-хостинга.zip»** с рабочего стола.
3. Дождаться окончания загрузки — 12 МБ, около минуты.

## Шаг 5. Распаковать

1. Выделить загруженный `АРКО-сайт-для-хостинга.zip`.
2. Нажать **«Извлечь»** («Распаковать»), путь распаковки — текущая папка.
3. После распаковки **удалить сам архив** — он больше не нужен.

Должно получиться: `index.html`, `styles.css`, `app.js`, `.htaccess`, `robots.txt`,
`sitemap.xml`, папки `assets` и `data`.

## Шаг 6. Проверить

Открыть **https://ar-ko.ru** в режиме инкогнито (Ctrl+Shift+N в Chrome) — обычное окно
может показать сохранённую старую версию.

Должен открыться новый сайт: тёмный первый экран с логотипом АРКО и картой ниже.

## Если что-то пошло не так

Вернуть старый сайт: удалить из папки `ar-ko.ru` всё новое, зайти в `_old`,
выделить всё, «Вырезать» → вернуться и «Вставить». Сайт станет прежним.

## После проверки

Удалить FTP-пользователя `u2210682_deploy` (раздел «FTP-аккаунты» в панели) — он был
создан разово, его пароль засветился в переписке.

Папку `_old` можно удалить через неделю-две, когда станет ясно, что всё работает.
